<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
                android:exported="true"
                android:launchMode="singleTask"
                android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Custom scheme: no verification possible (or needed) for a non-http(s) scheme,
                 so this stays unverified — see the App Link filter below for the real thing. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="pipeline" android:host="app"/>
                <data android:scheme="pipeline" android:host="list"/>
            </intent-filter>
            <!-- Real Android App Link: autoVerify here actually does something, since Android can
                 fetch https://pipeline.casaroja.es/.well-known/assetlinks.json to confirm we own
                 the domain. That file has to exist and be correct there for verification to pass
                 at install time — nothing in this repo hosts it. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https" android:host="pipeline.casaroja.es"/>
            </intent-filter>
        </activity>
    </application>

</manifest>